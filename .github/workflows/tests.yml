name: Automated tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  browser-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install test dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run browser tests
        run: npm test

      - name: Install AI Hackathon version dependencies
        run: npm ci --prefix v2

      - name: Install AI Hackathon version Chromium
        run: cd v2 && npx playwright install --with-deps chromium

      - name: Run AI Hackathon version checks
        run: |
          npm --prefix v2 run typecheck
          npm --prefix v2 run build
          npm --prefix v2 test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
